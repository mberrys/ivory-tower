# Adversarial fixture. The value below is this fixture's own sentinel, not a credential.
# Named deploy.env rather than .env so the repository .gitignore cannot hide the fixture.
DATABASE_URL=postgres://ivory:ivory@localhost:5432/ivory_tower
IVORY_S3_SECRET_ACCESS_KEY=FIXTURE_SENTINEL_DO_NOT_COMMIT_0a1b2c3d4e
